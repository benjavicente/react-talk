import dynamic from "next/dynamic";

export const App = dynamic(
	() => import("@/examples/share-state.tsx"),
	{ ssr: false }
);

## Compartir estado

```tsx file=<rootDir>/share-state.tsx

```

<div className="example">
	<App />
</div>
