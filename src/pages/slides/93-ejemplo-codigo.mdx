import { useState } from "react";

## Slide con código

export function Component() {
	const [state, setState] = useState(0);
	return (
		<div>
			<button
				className="btn"
				onClick={() => setState(state + 1)}
			>
				Increment
			</button>
			<p>Valor: {state}</p>
		</div>
	);
}

<div className="m-4 border-2 border-solid border-red-600 p-2">
	<Component />
</div>
